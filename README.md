### MIGRATION FROM ON PREM TO ADF

#### INTEGRATION RUNTIME
#### LINKED SERVICES
#### ADF: LOOKUP + FOR EACH
instead of using lookup activity, we could have used pipelines parameters with the full list of source files and gather them in the Settings -> items of the For Each activity but it's better with lookup
